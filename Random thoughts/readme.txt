Ok, this is another test.

Have finally managed to clone a github directory, manage it with git.
Creating this file to push it to github

And we have a GO !

Javascript
    return stop the execution of the block of code = exit of for/while blocks


Regex
    entouré de / (pas de guillemets simples ou doubles)
    \d = nombre
    /texte/ => cherche "texte"
